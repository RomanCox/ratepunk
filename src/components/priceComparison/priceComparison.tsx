import React from 'react';

import s from './priceComparison.module.scss';

export const PriceComparison = () => {
	return (
		<div className={s.mainWrapper}>
			<div className={s.mainContainer}>
				<h1 className={s.title}>PRICE COMPARISON PAGE</h1>
			</div>
		</div>
	);
};